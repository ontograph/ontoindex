# OntoIndex v2.1.2

## Fixed

- GitHub releases now attach the built `ontoindex-X.Y.Z.tgz` package consumed by both installer scripts.
- GitHub release publication now completes before npm publication, so npm registry failures no longer produce an assetless latest release.
