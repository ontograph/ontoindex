{
  "name": "ontoindex-native",
  "version": "0.0.0",
  "private": true,
  "description": "Optional native kernels for OntoIndex prototypes.",
  "main": "index.cjs",
  "scripts": {
    "build": "cargo build --release && node scripts/copy-native.cjs",
    "test": "npm run build && node test/native-csv-writer.test.cjs"
  },
  "files": [
    "Cargo.lock",
    "index.cjs",
    "native.cjs",
    "native",
    "scripts",
    "src",
    "Cargo.toml"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.7.1"
  },
  "license": "AGPL-3.0-or-later"
}
